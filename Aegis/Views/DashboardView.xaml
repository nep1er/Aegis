<UserControl x:Class="Aegis.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Выбор парковки -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Выберите парковку:" 
                       FontSize="16" 
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>
            <ComboBox ItemsSource="{Binding Parkings}"
                      SelectedItem="{Binding SelectedParking}"
                      DisplayMemberPath="Address"
                      Height="35"
                      FontSize="14"/>
        </StackPanel>

        <!-- Тарифы -->
        <Border Grid.Row="1" Background="#34495E" Padding="15" Margin="0,0,0,20" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Тарифы на парковку:" 
                           FontSize="16" 
                           FontWeight="Bold"
                           Foreground="White"
                           Margin="0,0,0,10"/>
                <ItemsControl ItemsSource="{Binding Tariffs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" Padding="10,5" Margin="5" CornerRadius="3">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding VehicleType}" FontWeight="Bold"/>
                                    <TextBlock Text=": " Margin="5,0"/>
                                    <TextBlock Text="{Binding Price, StringFormat={}{0} ₽/час}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Кнопка оформления -->
        <Button Grid.Row="1" 
                Content="📝 Оформить автомобиль"
                Height="45"
                HorizontalAlignment="Right"
                Width="250"
                Background="#27AE60"
                Foreground="White"
                FontSize="16"
                FontWeight="Bold"
                BorderThickness="0"
                Cursor="Hand"
                Command="{Binding NavigateToReceptionCommand}"
                Margin="0,8,19,29"/>

        <!-- Таблица мест -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Spots, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  Background="White"
                  BorderBrush="#BDC3C7"
                  BorderThickness="1">

            <!-- Стиль заголовков -->
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#34495E"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <!-- Стиль строк с цветовой индикацией -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                            <Setter Property="Background" Value="#FFE5E5"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsOccupied}" Value="False">
                            <Setter Property="Background" Value="#E8F8F5"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="№ Места" Binding="{Binding Number}" Width="100"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding VehicleType}" Width="150"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="120"/>
                <DataGridTextColumn Header="Гос. номер" Binding="{Binding LicensePlate}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>