<UserControl x:Class="Aegis.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <!-- Фильтры -->
            <Border Background="#ECF0F1" Padding="20" CornerRadius="5" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Аналитика" FontSize="28" FontWeight="Bold" Margin="0,0,0,15"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="С:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <DatePicker Grid.Column="1" SelectedDate="{Binding DateFrom}" Height="30" Width="150" Margin="0,0,20,0"/>
                        <TextBlock Grid.Column="2" Text="По:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <DatePicker Grid.Column="3" SelectedDate="{Binding DateTo}" Height="30" Width="150"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 1. Прибыль по месяцам -->
            <Border Background="White" Padding="20" CornerRadius="5" Margin="0,0,0,20">
                <StackPanel>
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Прибыль по месяцам" FontSize="20" FontWeight="Bold"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBlock Text="Парковка:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <ComboBox ItemsSource="{Binding Parkings}"
                                      SelectedItem="{Binding SelectedParkingForMonthlyChart}"
                                      DisplayMemberPath="Address"
                                      Width="250" Height="30"/>
                        </StackPanel>
                    </Grid>
                    <lvc:CartesianChart Height="300">
                        <lvc:CartesianChart.Series>
                            <lvc:ColumnSeries Title="Прибыль" Values="{Binding MonthlyRevenueValues}" Fill="#3498DB"/>
                        </lvc:CartesianChart.Series>
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Месяц" Labels="{Binding MonthLabels}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Рублей" LabelFormatter="{Binding FormatLabel}"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </StackPanel>
            </Border>

            <!-- 2. Прибыль по парковкам -->
            <Border Background="White" Padding="20" CornerRadius="5" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Прибыль по парковкам" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                    <lvc:CartesianChart Height="300">
                        <lvc:CartesianChart.Series>
                            <lvc:ColumnSeries Title="Прибыль" Values="{Binding ParkingRevenueValues}" Fill="#27AE60"/>
                        </lvc:CartesianChart.Series>
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Парковка" Labels="{Binding ParkingLabels}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Рублей" LabelFormatter="{Binding FormatLabel}"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </StackPanel>
            </Border>

            <!-- 3. Типы авто -->
            <Border Background="White" Padding="20" CornerRadius="5" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Распределение по типам автомобилей" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                    <lvc:PieChart Height="400" LegendLocation="Right" Series="{Binding VehicleTypeSeries}"/>
                </StackPanel>
            </Border>

            <!-- 4. Общая статистика -->
            <Border Background="White" Padding="20" CornerRadius="5" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Общая статистика" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="#E8F8F5" Padding="20" CornerRadius="5" Margin="5">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="Всего принято" FontSize="14" Foreground="#7F8C8D"/>
                                <TextBlock Text="{Binding ParkingStats.TotalReceived}" FontSize="32" FontWeight="Bold" Foreground="#27AE60"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Background="#FFF3CD" Padding="20" CornerRadius="5" Margin="5">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="На стоянке" FontSize="14" Foreground="#7F8C8D"/>
                                <TextBlock Text="{Binding ParkingStats.CurrentlyParked}" FontSize="32" FontWeight="Bold" Foreground="#E67E22"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Background="#EBF5FB" Padding="20" CornerRadius="5" Margin="5">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="Выдано" FontSize="14" Foreground="#7F8C8D"/>
                                <TextBlock Text="{Binding ParkingStats.Released}" FontSize="32" FontWeight="Bold" Foreground="#3498DB"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 5. По городам -->
            <Border Background="White" Padding="20" CornerRadius="5" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Статистика по городам" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                    <lvc:CartesianChart Height="300">
                        <lvc:CartesianChart.Series>
                            <lvc:ColumnSeries Title="Выручка" Values="{Binding CityRevenueValues}" Fill="#9B59B6"/>
                        </lvc:CartesianChart.Series>
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Город" Labels="{Binding CityLabels}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Рублей" LabelFormatter="{Binding FormatLabel}"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </StackPanel>
            </Border>

            <!-- 6. Количество парковок -->
            <Border Background="White" Padding="20" CornerRadius="5" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Количество парковок по городам" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                    <lvc:CartesianChart Height="300">
                        <lvc:CartesianChart.Series>
                            <lvc:ColumnSeries Title="Количество" Values="{Binding CityParkingCountValues}" Fill="#E67E22"/>
                        </lvc:CartesianChart.Series>
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Город" Labels="{Binding CityLabels}"/>
                        </lvc:CartesianChart.AxisX>
                    </lvc:CartesianChart>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>