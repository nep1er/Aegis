<Window x:Class="Aegis.Views.EditUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование пользователя" 
        Height="612" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <StackPanel Margin="30">
        <TextBlock Text="Редактирование" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Text="ФИО:" FontWeight="Bold" Foreground="#7F8C8D"/>
        <TextBlock x:Name="TxtFullName" FontSize="16" Margin="0,5,0,15"/>

        <TextBlock Text="Логин:" FontWeight="Bold" Foreground="#7F8C8D"/>
        <TextBlock x:Name="TxtLogin" FontSize="16" Margin="0,5,0,15"/>

        <TextBlock Text="Роль *" Margin="0,0,0,5"/>
        <ComboBox x:Name="CmbRole" Height="35" Margin="0,0,0,15"/>

        <TextBlock Text="Назначенные парковки:" Margin="0,0,0,5"/>
        <ListBox x:Name="LstParkings" Height="150" Margin="0,0,0,15" SelectionMode="Multiple"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Width="120" Height="40" Margin="0,0,10,0"
                    Background="#95A5A6" Foreground="White"
                    Click="Cancel_Click"/>
            <Button Content="Сохранить" Width="120" Height="40"
                    Background="#3498DB" Foreground="White"
                    FontWeight="Bold"
                    Click="Save_Click"/>
        </StackPanel>
    </StackPanel>
</Window>