<Window x:Class="Aegis.Views.CreateUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Создание пользователя" 
        Height="600" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30">
            <TextBlock Text="Создание учётной записи" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Логин *" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtLogin" Height="35" Margin="0,0,0,15" Padding="8"/>

            <TextBlock Text="Пароль *" Margin="0,0,0,5"/>
            <PasswordBox x:Name="TxtPassword" Height="35" Margin="0,0,0,15" Padding="8"/>

            <TextBlock Text="ФИО" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtFullName" Height="35" Margin="0,0,0,15" Padding="8"/>

            <TextBlock Text="Телефон" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtPhone" Height="35" Margin="0,0,0,15" Padding="8"/>

            <TextBlock Text="Роль *" Margin="0,0,0,5"/>
            <ComboBox x:Name="CmbRole" Height="35" Margin="0,0,0,15"/>

            <TextBlock Text="Назначенные парковки:" Margin="0,0,0,5"/>
            <ListBox x:Name="LstParkings" Height="150" Margin="0,0,0,15" SelectionMode="Multiple"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" Width="120" Height="40" Margin="0,0,10,0"
                        Background="#95A5A6" Foreground="White"
                        Click="Cancel_Click"/>
                <Button Content="Создать" Width="120" Height="40"
                        Background="#27AE60" Foreground="White"
                        FontWeight="Bold"
                        Click="Create_Click"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>